% \section{Related Work and Background}
%

\section{Related Work}
 \label{sec:related_work}
\paragraph{Federated Reinforcement Learning with Shared Parameterizations.}
Fed - DQN 

\paragraph{Distillation-Based Federated RL for Heterogeneous Agents.}
\cite{hinton2015distilling} introduced the concept of dark knowledge: the information encoded in a teacher's soft probability distribution over classes that reveals inter-class similarities invisible in hard labels.

\cite{rusu2016policydistillation}  authored the foundational transfer of knowledge distillation to deep RL with policy distillation by mapping DQN's Q-value vector to logits and apply temperature-scaled softmax to yield a soft action distribution.

Instead of sharing parameters between the different workers, \cite{teh2017distral} proposes to share a distilled policy that captures common behavior across tasks, FedDF \cite{lin2021ensembledistillation} received client models form an ensemble teacher, and a student global model is trained on an unlabeled server-side dataset

\paragraph{Black-Box and Query-Based Federated Q-Learning.}  FHQL 


\paragraph{Heterogeneous Environments and Tasks in Federated RL.} In reward, environment not structure 



% \cite{ni2022qhd} proposes a hyperdimensional regression model to approximate the Q-value function.

\cite{ye2023fedfm} anchor based feature matching FRL

\cite{jin2022federated}  Federated Reinforcement Learning with Environment Heterogeneity (FRL-EH) framework

\cite{hwang2025fed} Federated Reinforcement Learning with Environment Heterogeneity (FRL-EH) framework by incorporating a regularization term in the local update


\paragraph{Proposed FedQHD.}
In contrast, \textbf{FedQHD} targets \emph{structural heterogeneity}, where clients may use different encoders and different parameter dimensions, rendering parameter-space synchronization algebraically undefined.
FedQHD replaces parameter averaging with a \emph{function-space} federation primitive based on a shared anchor interface: client models are aggregated via Q-value consensus on anchor states, and the resulting global teacher is compiled back into each client’s private representation through a \emph{one-shot closed-form ridge or proximal-ridge projection}. This structure avoids iterative distillation, remains well-defined under mixed encoders and dimensions, and admits a homogeneous limit in which the method reduces exactly to FedAvg, providing a principled bridge between classical federated learning and heterogeneous federation.



